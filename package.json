{
  "private": true,
  "scripts": {
    "build:assets": "node scripts/build-assets.mjs",
    "prune:assets": "node scripts/prune-dev-assets.mjs",
    "deploy:assets": "npm run build:assets && npm run prune:assets"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
